# Gradle / build artifacts
.gradle/
build/
app/build/
payload/build/
local.properties
.kotlin/
captures/

# Never commit the real relay secret
**/BuildConfig.java

# Release keystore (private signing key)
# CRITICAL: We DO NOT want the target (or scanners) finding our signing key
relay-release.keystore
*.keystore
*.jks

# Android Studio / IDE
*.iml
.idea/
.artifacts/
.navigation/

# Secrets / Environment
gradle.properties
